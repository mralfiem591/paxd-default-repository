name: Update Search Index

on:
  push:
    branches:
      - main
    paths:
      - 'packages/**'
      - 'resolution'
  
  workflow_dispatch:

env:
  REPO_DIR: /srv/dev-disk-by-uuid-C8EC4407EC43EDEE/General/actions-runner/_work/paxd/paxd

jobs:
  update-index:
    runs-on: self-hosted
    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          path: ${{ env.REPO_DIR }}
          clean: false

      - name: Set up Python virtual environment
        working-directory: ${{ env.REPO_DIR }}
        run: uv venv

      - name: Install dependencies
        working-directory: ${{ env.REPO_DIR }}
        run: uv pip install pyyaml

      - name: Generate search index
        working-directory: ${{ env.REPO_DIR }}
        run: uv run generate_searchindex.py

      - name: Check for changes
        id: git-check
        working-directory: ${{ env.REPO_DIR }}
        run: git diff --exit-code -- searchindex.csv || echo "changed=true" >> $GITHUB_OUTPUT

      - name: Commit and push if changed
        if: steps.git-check.outputs.changed == 'true'
        working-directory: ${{ env.REPO_DIR }}
        run: |
          git config --local user.email "github-actions[bot]@users.noreply.github.com"
          git config --local user.name "github-actions[bot]"
          git add searchindex.csv
          git commit -m "Update search index"
          git push
